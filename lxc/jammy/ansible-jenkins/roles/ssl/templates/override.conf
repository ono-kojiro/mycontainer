[Service]
Environment="JENKINS_HTTP_PORT={{ jenkins_http_port }}"
Environment="JENKINS_HTTPS_PORT={{ jenkins_https_port }}"
Environment="JENKINS_HTTPS_KEYSTORE={{ jenkins_https_keystore }}"
Environment="JENKINS_HTTPS_KEYSTORE_PASSWORD={{ jenkins_https_keystore_password }}"

