- hosts: all

  tasks:
    - debug: msg="Hello World"
    - debug: msg="msg is {{ msg }}"
    - debug: msg="variable0 is {{ variable0 }}"
    - debug: msg="variable1 is {{ variable1 }}"
 
  post_tasks:
    - name: post task
      debug:
        msg='finished'

