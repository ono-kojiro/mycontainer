- name: uninstall slapd packages
  apt :
    name: slapd
    state: absent
    purge: yes
  
- name: install slapd packages
  apt:
    name:
      - slapd
      - ldap-utils

- name: add user 'openldap' to group 'ssl-cert'
  user:
    name: 'openldap'
    groups: 'ssl-cert'
    append: yes

