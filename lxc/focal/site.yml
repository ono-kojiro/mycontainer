- hosts: targets

  roles:
    - role: certs
      tags: certs
    - role: slapd
      tags: slapd
    - role: httpd
      tags: httpd

  post_tasks:
    - name: finished.
      debug:
        msg='finished installing slapd'

