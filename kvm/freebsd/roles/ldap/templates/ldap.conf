#
# LDAP Defaults
#

# See ldap.conf(5) for details
# This file should be world readable but not world writable.

BASE    {{ ldap_suffix }}
URI     {{ ldap_uri }}

TLS_REQCERT never

#SIZELIMIT	12
#TIMELIMIT	15
#DEREF		never
