- name: install sudo
  shell: |
    pkg install -y sudo

- name: add sss for nsswitch group
  template:
    src: ldapwheel.conf
    dest: /usr/local/etc/sudoers.d
    mode: '0644'

- name: add sudo for pam
  template:
    src: sudo
    dest: /etc/pam.d/sudo
    mode: '0644'

