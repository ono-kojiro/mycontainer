- hosts: all
  gather_facts: no

  roles:
    - role: macvlan
      tags: macvlan

    - role: docker
      tags: docker
 
  post_tasks:
    - name: finished.
      debug:
        msg='finished macvlan config'

