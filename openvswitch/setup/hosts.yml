all:
  children:
    servers:
      hosts:
        myserver:
          ansible_become: true
          ansible_host: 127.0.0.1
          ansible_user: '{{ lookup(''ansible.builtin.env'', ''USER'') }}'
        
        alma:
          ansible_become: true
          ansible_host: alma
          ansible_user: '{{ lookup(''ansible.builtin.env'', ''USER'') }}'

