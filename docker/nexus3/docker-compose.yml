# Use root/example as user/password credentials
version: '3.1'

services:

  couchdb:
    image: sonatype/nexus3
    container_name: nexus
    hostname: nexus
    restart: always
    ports:
      - 8081:8081

