version: '3'
services:
  jammy:
    image: jammy:latest
    network_mode: bridge
    container_name: jammy
    hostname: jammy
    restart:  always
    stdin_open: true   # docker run -i
    tty:        true   # docker run -t

