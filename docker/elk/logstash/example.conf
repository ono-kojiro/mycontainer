# Sample Logstash configuration for creating a simple
# Beats -> Logstash -> Elasticsearch pipeline.

input {
  file {
    mode => "tail"
    path => [ "/home/data/csv/*.csv" ]
    sincedb_path => "/home/data/sincedb"
    start_position => "beginning"
    codec => plain {
      charset => "UTF-8"
    }
  }
}

output {
  elasticsearch {
    hosts => ["https://localhost:9200"]
    index => "myindex"
    user => "elastic"
    password => "5V6OkyqZ=opprQDAVxtA"
    ssl => true
    ssl_certificate_verification => false
  }
}

