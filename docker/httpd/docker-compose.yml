# Use root/example as user/password credentials
version: '3.1'

services:

  couchdb:
    image: httpd:2.4.55
    container_name: httpd
    hostname: httpd
    restart: always
    ports:
      - 18080:80
      - 18443:443



