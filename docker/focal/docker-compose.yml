version: '3'
services:
  focal:
    image: focal-sphinx
    container_name: sphinx
    hostname: sphinx
    restart:  always
    stdin_open: true   # docker run -i
    tty:        true   # docker run -t
    ports:
      - "20041:22"


