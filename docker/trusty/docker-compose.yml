version: '3'
services:
  trusty:
    image: trusty:latest
    network_mode: bridge
    container_name: trusty
    hostname: trusty
    restart:  always
    stdin_open: true   # docker run -i
    tty:        true   # docker run -t
    ports:
      - "14040:22"

