- name: deploy grafana
  hosts:
  - luna2

  #environment:
  #  http_proxy: http://proxy.example.com:8080
  #  https_proxy: http://proxy.example.com:8080

  roles:
  - role: grafana
    tags: grafana

  - role: destroy
    tags: destroy, never

