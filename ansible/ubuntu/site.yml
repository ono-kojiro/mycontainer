- name: setup ubuntu
  hosts: myserver

  #environment:
  #  http_proxy: http://proxy.example.com:8080
  #  https_proxy: http://proxy.example.com:8080

  roles:
  - role: cacert
    tags: cacert
  
  - role: cert
    tags: cert

  - role: sssd
    tags: sssd

  - role: sudo
    tags: sudo

