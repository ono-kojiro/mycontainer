all:
  children:
    servers:
      hosts:
        myserver:
          ansible_become: true
          ansible_become_exe: /usr/bin/sudo
          ansible_host: localhost
          server_crt: nginx.crt
          server_key: nginx.key
          server_name: couchdb
