- name: setup nginx
  hosts:
  - localhost
  
  #environment:
  #  http_proxy: http://proxy.example.com:8080
  #  https_proxy: http://proxy.example.com:8080

  roles:
  - name: nginx
    tags: nginx

  - name: ssl
    tags: ssl

  - name: couchdb
    tags: couchdb
  
  - name: api_key
    tags: api_key

  #- name: client
  #  tags: client, never
  
  #- name: grafana
  #  tags: grafana, never

  - name: destroy
    tags: destroy, never

