- name: setup nginx
  hosts: myserver
  
  #environment:
  #  http_proxy: http://proxy.example.com:8080
  #  https_proxy: http://proxy.example.com:8080

  roles:
  - name: nginx
    tags: nginx

  - name: ssl
    tags: ssl
  
  - name: api_key
    tags: api_key, never

  - name: couchdb
    tags: couchdb, never

  - name: grafana
    tags: grafana, never

  - name: destroy
    tags: destroy, never

