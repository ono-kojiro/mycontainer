- name: setup filebeat
  hosts: filebeat
  
  #environment:
  #  http_proxy: http://proxy.example.com:8080
  #  https_proxy: http://proxy.example.com:8080

  roles:
    - role: certs
      tags: certs, never

    - role: filebeat
      tags: filebeat
    
    - role: example
      tags: example, never
    
    - role: multiline
      tags: multiline, never
    
    - role: csv
      tags: csv, never

    - { role: pcapd, tags: [pcapd, never] }

