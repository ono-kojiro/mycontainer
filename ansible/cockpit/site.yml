- name: setup cockpit
  hosts:
    - myserver

  #environment:
  #  http_proxy: http://proxy.example.com:8080
  #  https_proxy: http://proxy.example.com:8080

  roles:
  - role: cockpit
    tags: cockpit

  - role: nginx
    tags: nginx

