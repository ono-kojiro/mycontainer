- name: setup logstash
  hosts: logstash
  
  #environment:
  #  http_proxy: http://proxy.example.com:8080
  #  https_proxy: http://proxy.example.com:8080

  roles:
  - role: logstash
    tags: logstash

  - role: example
    tags: example

