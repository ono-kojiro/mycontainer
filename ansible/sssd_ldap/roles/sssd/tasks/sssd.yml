---
- name: enable epel-release (OracleLinux)
  ansible.builtin.dnf:
    name:
      - "{{ epel_package }}"
    state: present
  when: ansible_facts['distribution'] == "OracleLinux"

- name: install sssd-ldap for debian
  ansible.builtin.apt:
    name:
      - sssd-ldap
      - sssd-tools
      - oddjob-mkhomedir
  when: ansible_facts['os_family'] == "Debian"

- name: install sssd-ldap for centos
  ansible.builtin.dnf:
    name:
      - openldap-clients
      - sssd
      - sssd-ldap
      - sssd-tools
      - authselect
      - oddjob-mkhomedir
  when: |-
    ansible_facts['os_family'] == "RedHat"

- name: install sssd (FreeBSD)
  ansible.builtin.package:
    name:
    - bash
    - sssd2
    - pam_mkhomedir
    state: present
  when: ansible_facts['os_family'] == "FreeBSD"

- name: install sssd (ArchLinux)
  ansible.builtin.package:
    name:
    - sssd
    state: present
  when: ansible_facts['os_family'] == "Archlinux"

- name: create symlink of bash (FreeBSD)
  ansible.builtin.file:
    src:  /usr/local/bin/bash
    dest: /bin/bash
    state: link
  when: ansible_facts['os_family'] == "FreeBSD"

- name: install openldap (FreeBSD)
  community.general.pkgng:
    name:
    - openldap26-client
    state: present
  when: ansible_facts['os_family'] == "FreeBSD"

- name: install openldap (ArchLinux)
  ansible.builtin.package:
    name:
    - openldap
    state: present
  when: ansible_facts['os_family'] == "Archlinux"
  
- name: check if sssd is running
  ansible.builtin.systemd:
    name:  sssd
    state: started
  ignore_errors: true
  when: |-
    ansible_facts['service_mgr'] == "systemd"

- name: copy sssd.conf
  ansible.builtin.template:
    src: sssd.conf
    dest: "{{ sysconfdir }}/sssd/sssd.conf"
    owner: "{{ sssd_owner }}"
    group: "{{ sssd_group }}"
    mode: '0600'
  when: |-
    ansible_facts['os_family'] != "FreeBSD"

- name: copy sssd.conf (FreeBSD)
  ansible.builtin.template:
    src: sssd.conf
    dest: "{{ sysconfdir }}/sssd/sssd.conf"
    owner: "{{ sssd_owner }}"
    mode: '0600'
  when: |-
    ansible_facts['os_family'] == "FreeBSD"

- name: enable sss_ssh_authorizedkeys
  ansible.builtin.lineinfile:
    path: /etc/ssh/sshd_config
    regexp: '^#?AuthorizedKeysCommand '
    line: AuthorizedKeysCommand {{ authorizedkeyscommand }}

- name: enable AuthorizedKeysCommandUser
  ansible.builtin.lineinfile:
    path: /etc/ssh/sshd_config
    regexp: '^#?AuthorizedKeysCommandUser '
    line: 'AuthorizedKeysCommandUser nobody'

- name: enable sssd
  ansible.builtin.systemd:
    name: sssd
    enabled: yes
    masked: no
    daemon_reload: yes
  when: |-
    ansible_facts['service_mgr'] == "systemd"

- name: enable sssd (Freebsd)
  community.general.sysrc:
    name: sssd_enable
    value: "YES"
    path: /etc/rc.conf
  when: |-
    ansible_facts['os_family'] == "FreeBSD"

- name: restart oddjobd
  ansible.builtin.systemd:
    name: oddjobd
    state: restarted
    daemon_reload: yes
  when: |-
    ansible_facts['os_family'] == "RedHat"

- name: enable mkhomedir for Debian
  shell: |
    pam-auth-update --enable mkhomedir --force
  when: ansible_facts['os_family'] == "Debian"

- name: enable mkhomedir for RedHat
  ansible.builtin.shell: |
    authselect select sssd with-sudo with-mkhomedir --force
    systemctl enable --now oddjobd.service
  when: |-
    ansible_facts['os_family'] == "RedHat"

- name: run tasks for FreeBSD
  ansible.builtin.import_tasks: sssd-freebsd.yml
  when: |-
    ansible_facts['os_family'] == "FreeBSD"

- name: run pam tasks for ArchLinux
  ansible.builtin.import_tasks: sssd_archlinux.yml
  when: |-
    ansible_facts['os_family'] == "Archlinux"


- name: restart sssd
  ansible.builtin.service:
    name: sssd
    state: restarted

- name: restart sshd
  ansible.builtin.service:
    name:  "{{ sshd_service }}"
    state: restarted

