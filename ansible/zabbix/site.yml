- name: setup localhost
  hosts:
    - localhost

  #environment:
  #  http_proxy: http://proxy.example.com:8080
  #  https_proxy: http://proxy.example.com:8080

  roles:
  - name: apache
    tags: apache

  - name: ssl
    tags: ssl

  - role: zabbix
    tags: zabbix

