---
- name: install apache
  ansible.builtin.package:
    name: apache2
    state: present

- name: enable mod-ssl
  ansible.builtin.shell: |
    a2enmod ssl

- name: restart apache2
  ansible.builtin.systemd_service:
    name: apache2
    state: restarted

