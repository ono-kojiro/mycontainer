- name: setup packetbeat
  hosts: packetbeat
  
  #environment:
  #  http_proxy: http://proxy.example.com:8080
  #  https_proxy: http://proxy.example.com:8080

  roles:
    - role: packetbeat
      tags: packetbeat
    
